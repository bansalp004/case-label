version: '3'
services:  
  api:
    build: ./user-service
    ports:
      - '8080:8080'
    depends_on:
      - mongo
  case-service:
      build: ./case-service
      ports:
        - '8081:8081'
      depends_on:
        - mongo    
  mongo:
    image: mongo
    ports:
      - '27017:27017'
